import {atom} from 'recoil'

export const ModalState = atom({
  key: "ModalState",
  default: false
})

export const movesDaynmic = atom({
  key: " movesDaynmic",
  default: null
})
export const rowmovies = atom({
  key: "rowmovies",
  default: 'flase'
})

export const volumePlayer = atom({
  key: 'volumePlayer',
  default: false
})
